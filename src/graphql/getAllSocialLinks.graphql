query getAllSocialLinks {
    allSocialLinks(
        sort: {
            orderRank: ASC
        }
    ) {
        _key
        title
        url
        socialIcon {
            _key
            _type
            asset {
                altText
                url
            }
        }
    }
}