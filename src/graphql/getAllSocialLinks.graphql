query getAllSocialLinks {
    allSocialLinks(
        sort: {
            orderRank: ASC
        }
    ) {
        _id
        _key
        title
        url
        socialIcon {
            _key
            _type
            asset {
                altText
                url
            }
        }
    }
}